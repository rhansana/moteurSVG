//
// Created by hansana on 24/02/2020.
//

#include "client.h"
#include <iostream>

int main() {
    Client c1;
    /**std::string hello = "\n";
    std::getline(std::cin,hello);
    const char *c = hello.c_str();
    c1.sendMsg(c);
     */
     c1.sendData(c1.test());
    return 0;
}